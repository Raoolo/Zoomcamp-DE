1a_Docker_SQL/ny_taxi_postgres_data/
1a_Docker_SQL/.ipynb_checkpoints/
1a_Docker_SQL/config/
1b_Terraform/keys/
1c_Homework/ny_taxi_postgres_data/
1c_Homework/config/

# Local .terraform directories
**/.terraform/*
# the ** is a wildcard that matches all directories, even higher up the tree and nested
# the * is a wildcard that matches all files and directories, but not nested and not higher up the tree

# Ignore variables files
*.auto.tfvars

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json


# Ignore environment-specific files
.envrc

# Ignore CLI configuration files
.terraformrc
terraform.rc

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Ignore sensitive files
*.pem
*.key
*.pub